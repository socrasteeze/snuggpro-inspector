{
  "name": "snuggpro-inspector",
  "version": "1.0.0",
  "description": "Browser UI to inspect SnuggPro jobs and flatten measures/usage for reporting — runs locally via proxy.js or hosted as a Cloudflare Worker with email-code login.",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  },
  "license": "UNLICENSED",
  "private": true
}
